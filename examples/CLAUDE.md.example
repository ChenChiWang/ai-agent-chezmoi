# 全域偏好(範例)

> 這是 `~/.claude/CLAUDE.md` 的通用範本,示範可以放哪些內容。
> 請依自己的習慣調整,不要照抄。

## 語言
- 回應使用的語言(例如:繁體中文 / English)
- 程式碼註解語言
- commit message 語言與規範(例如:遵循 Conventional Commits)

## 程式風格
- 偏好簡潔、避免過度封裝
- 錯誤處理要完整,不要吞掉 error
- 設定第三方服務版本時,先確認官方最新相容組合

## 技術偏好
- 常用語言 / 框架 / 套件的預設選擇

## 互動偏好
- 不確定時先詢問,不要自行猜測
- 修改檔案前先說明要改什麼
- 大範圍重構前先列出計畫確認

## 安全與操作習慣
- `.env`、credentials、API keys 不進 git,新專案先設好 `.gitignore`
- 破壞性操作(`rm -rf`、`DROP TABLE`、`git reset --hard`)前先確認或備份
